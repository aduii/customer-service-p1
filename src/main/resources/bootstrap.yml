server:
  port: 0

spring:
  application:
    name: customer-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      profile: dev

eureka:
  instance:
    instance-id: ${spring.application.name}:${random.uuid}
management:
  endpoints:
    web:
      exposure:
        include: "*"